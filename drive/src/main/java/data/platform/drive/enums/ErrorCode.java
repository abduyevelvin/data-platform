package data.platform.drive.enums;

public enum ErrorCode {
    NOT_FOUND,
    ALREADY_EXISTS,
    DRIVE_ERROR,
    INVALID_DESTINATION,
    FILE_SIZE_EXCEEDED,
    SAME_SOURCE_AND_DESTINATION,
    INTERNAL_SERVER_ERROR
}
